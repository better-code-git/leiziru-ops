<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ureactor.jeesite.modules.advertisement.dao.AdvertisementDao">
    
	<sql id="advertisementColumns">
		a.id AS "id",
		a.ad_pic_url  AS "adPicUrl",
		a.product_id  AS "productId",
		a.ad_position AS "adPosition",
		a.create_date AS "createDate",
		a.update_date AS "updateDate"
	</sql>
	
	<sql id="advertisementJoins">
	</sql>
    
	<select id="get" resultType="Advertisement">
		SELECT 
			<include refid="advertisementColumns"/>
		FROM lzr_advertisement a
		<include refid="advertisementJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Advertisement">
		SELECT 
			<include refid="advertisementColumns"/>
		FROM lzr_advertisement a
		<include refid="advertisementJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="productId != null and productId != ''">
				AND a.product_id = #{productId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	
	<select id="findListByCheck" resultType="Advertisement">
		SELECT 
			<include refid="advertisementColumns"/>
		FROM lzr_advertisement a
		<include refid="advertisementJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="id != null and id != ''">
				AND a.id != #{id}
			</if>
			<if test="productId != null and productId != ''">
				AND a.product_id = #{productId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	
	
	
	<select id="findAllList" resultType="Advertisement">
		SELECT 
			<include refid="advertisementColumns"/>
		FROM lzr_advertisement a
		<include refid="advertisementJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO lzr_advertisement(
			ad_pic_url,
			product_id,
			ad_position,
			create_date,
			update_date
		) VALUES (
			#{adPicUrl},
			#{productId},
			#{adPosition},
			#{createDate},
			#{updateDate}		)
	</insert>
	
	<update id="update">
		UPDATE lzr_advertisement SET 	
			ad_pic_url = #{adPicUrl},
			product_id = #{productId},
			ad_position = #{adPosition},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE lzr_advertisement SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>