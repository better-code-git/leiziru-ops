<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ureactor.jeesite.modules.product.dao.ProductBlockPriceDao">
    
	<sql id="productBlockPriceColumns">
		a.id AS "id",
		a.block_id AS "blockId",
		a.room_type AS "roomType",
		a.avage_price AS "avagePrice",
		a.product_id AS "productId",
		a.low_price AS "lowPrice",
		a.belong_month AS "belongMonth",
		a.district_name AS "districtName",
		a.create_date AS "createDate",
		a.update_date AS "updateDate"
	</sql>
	
	<sql id="productBlockPriceJoins">
	</sql>
    
	<select id="get" resultType="ProductBlockPrice">
		SELECT 
			<include refid="productBlockPriceColumns"/>
		FROM lzr_product_block_price a
		<include refid="productBlockPriceJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ProductBlockPrice">
		SELECT 
			<include refid="productBlockPriceColumns"/>
		FROM lzr_product_block_price a
		<include refid="productBlockPriceJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="productId != null and productId != ''">
				AND a.product_id=#{productId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ProductBlockPrice">
		SELECT 
			<include refid="productBlockPriceColumns"/>
		FROM lzr_product_block_price a
		<include refid="productBlockPriceJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO lzr_product_block_price(
			block_id,
			room_type,
			avage_price,
			product_id,
			low_price,
			belong_month,
			district_name,
			create_date,
			update_date
		) VALUES (
			#{blockId},
			#{roomType},
			#{avagePrice},
			#{productId},
			#{lowPrice},
			#{belongMonth},
			#{districtName},
			#{createDate},
			#{updateDate}
		)
	</insert>
	
	<update id="update">
		UPDATE lzr_product_block_price SET 	
			block_id  =#{blockId},
			room_type  =#{roomType},
			avage_price  =#{avagePrice},
			product_id  =#{productId},
			low_price  =#{lowPrice},
			belong_month  =#{belongMonth},
			district_name  =#{districtName},
			update_date =#{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE lzr_product SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>