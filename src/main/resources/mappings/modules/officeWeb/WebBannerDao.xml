<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ureactor.jeesite.modules.officeWeb.dao.WebBannerDao">
    
	<sql id="webBannerColumns">
		a.id AS "id",
		a.web_banner_url  AS "webBannerUrl",
		a.sort AS "sort",
		a.create_date AS "createDate",
		a.update_date AS "updateDate"
	</sql>
	
	<sql id="webBannerJoins">
	</sql>
    
	<select id="get" resultType="WebBanner">
		SELECT 
			<include refid="webBannerColumns"/>
		FROM lzr_web_banner a
		<include refid="webBannerJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="WebBanner">
		SELECT 
			<include refid="webBannerColumns"/>
		FROM lzr_web_banner a
		<include refid="webBannerJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="WebBanner">
		SELECT 
			<include refid="webBannerColumns"/>
		FROM lzr_web_banner a
		<include refid="webBannerJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO lzr_web_banner(
			id,
			web_banner_url,
			sort,
			create_date,
			update_date
		) VALUES (
			#{id},
			#{webBannerUrl},
			#{sort},
			#{createDate},
			#{updateDate}		)
	</insert>
	
	<update id="update">
		UPDATE lzr_web_banner SET 	
			web_banner_url = #{webBannerUrl},
			sort = #{sort},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE lzr_web_banner SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>