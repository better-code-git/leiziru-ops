<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ureactor.jeesite.modules.officeWeb.dao.CompanyDao">
    
	<sql id="companyColumns">
		a.id AS "id",
		a.company_profile AS "companyProfile",
		a.create_date AS "createDate",
		a.update_date AS "updateDate"
	</sql>
	
	<sql id="companyJoins">
	</sql>
    
	<select id="get" resultType="Company">
		SELECT 
			<include refid="companyColumns"/>
		FROM lzr_company a
		<include refid="companyJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Company">
		SELECT 
			<include refid="companyColumns"/>
		FROM lzr_company a
		<include refid="companyJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Company">
		SELECT 
			<include refid="companyColumns"/>
		FROM lzr_company a
		<include refid="companyJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO lzr_company(
			company_profile,
			create_date,
			update_date
		) VALUES (
			#{companyProfile},
			#{createDate},
			#{updateDate}		)
	</insert>
	
	<update id="update">
		UPDATE lzr_company SET 	
			company_profile = #{companyProfile},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE lzr_company SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>