<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ureactor.jeesite.modules.order.dao.OrderCommissionDao">
    
	<sql id="orderCommissionColumns">
		a.id AS "id",
		a.create_date AS "createDate",
		a.update_date AS "updateDate"
	</sql>
	
	<sql id="orderCommissionJoins">
	</sql>
    
	<select id="get" resultType="OrderCommission">
		SELECT 
			<include refid="orderCommissionColumns"/>
		FROM lzr_order_commission a
		<include refid="orderCommissionJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="OrderCommission">
		SELECT 
			<include refid="orderCommissionColumns"/>
		FROM lzr_order_commission a
		<include refid="orderCommissionJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="OrderCommission">
		SELECT 
			<include refid="orderCommissionColumns"/>
		FROM lzr_order_commission a
		<include refid="orderCommissionJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO lzr_order_commission(
			order_id,
			order_time,
			order_amount,
			product_id,
			product_name,
			order_status,
			order_type,
			member_id,
			create_date,
			update_date
		) VALUES (
			#{orderId},
			#{orderTime},
			#{orderAmount},
			#{productId},
			#{productName},
			#{orderStatus},
			#{orderType},
			#{memberId},
			#{createDate},
			#{updateDate},
		)
	</insert>
	
	<update id="update">
		UPDATE lzr_order_commission SET 	
			order_id =#{orderId},
			order_time =#{orderTime},
			order_amount =#{orderAmount},
			product_id =#{productId},
			product_name =#{productName},
			order_status =#{orderStatus},
			order_type =#{orderType},
			member_id =#{memberId},
			update_date =#{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE lzr_order_commission SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
	<!--逻辑删除批量-->
	<update id="deleteBatchByLogic">
		UPDATE lzr_order_commission SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id in
		<foreach collection="idList" item="id" open="(" close=")" separator=",">
				  #{id}
		</foreach> 
	</update>
</mapper>